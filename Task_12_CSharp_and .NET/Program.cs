using System;

namespace Task_12_CSharp_and_.NET
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("The differences between C# & .NET Framework:");
            Console.WriteLine(".NET isn't just a library, but also a runtime for executing applications." + Environment.NewLine + "The knowledge of C# implies some knowledge of .NET (because the C# object model corresponds to the .NET object model and you can do something interesting in C# just by using .NET libraries). The opposite isn't necessarily true as you can use other languages to write .NET applications.");
        }
    }
}
